server:
  requestLog:
    appenders: []

logging:
  level: INFO
  loggers:
    "com.wire.bots.logger": INFO

swagger:
  resourcePackage: com.wire.bots.hold.resource
  uriPrefix: /legalhold
  title: Legal Hold Service
  description: Legal Hold Service documentation
  contact: Dejan Kovačević
  contactEmail: dejan@wire.com
  contactUrl: "wire://user/eda5984e-3a23-495e-9110-73ed905876cf"
  schemes:
    - http
    - https

jerseyClient:
  timeout: 20s
  connectionTimeout: 4000ms
  connectionRequestTimeout: 4000ms
  keepAlive: 0ms
  retries: 3

auth: ${HOLD_SERVICE_TOKEN}

baseUrl: ${HOLD_BASE_URL}

#redis DB
db:
  driver: redis
  host: localhost
  port: 6379    #redis default port

storage:
  host: localhost
  port: 5432
  database: postgres
  driver: postgresql
#  user: admin
#  password: ${POSTGRES_PASSWORD}

